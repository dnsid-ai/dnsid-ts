---
title: "TypeScript: @dnsid-ai/sdk"
description: "Runtime-neutral SDK entry point; inject DNS, fetch, and key-provider implementations."
---

<!-- Generated by `npm run docs:md` from TSDoc comments. Do not edit by hand; see docs/AGENTS.md. -->

Ergonomic aggregate package for DNSid TypeScript consumers.

The root `@dnsid-ai/sdk` entrypoint is runtime-neutral: callers inject DNS resolution, JSON fetching, key-provider, cache, and log implementations. Node.js defaults live behind the `@dnsid-ai/sdk/node` subpath and optional `@dnsid-ai/transport` peer.

## Package layout

- **Root export (`@dnsid-ai/sdk`)** — runtime-neutral. No Node built-ins, `Buffer`, filesystem, or `undici` imports; you inject `dnsResolver`, `fetchJson`, and key providers. Safe to bundle for browsers and other non-Node runtimes. Its managed C2SP workflows use the portable writer-only binding entrypoint.
- **`@dnsid-ai/sdk/node` subpath** — Node conveniences: `LocalKeyProvider`, `configFromEnvironment`, `createNodeIdentityManager`, `createNodeIdentityManagerFromDnsid`. Loads the optional `@dnsid-ai/transport` peer when HTTPS defaults are needed.
- **OIDC lives in `@dnsid-ai/oidc`** — deliberately not re-exported from the root because its default transport is Node-bound, and private-key token minting belongs server-side. Import it directly.

## Install

```sh
npm install @dnsid-ai/sdk
```

For Node defaults:

```sh
npm install @dnsid-ai/sdk @dnsid-ai/transport
```

## Runtime-neutral usage

```ts
import { createIdentityManager } from '@dnsid-ai/sdk';

const idm = createIdentityManager({ identity, verification }, {
  keyProvider,       // operational ku key
  entityKeyProvider, // accountable-entity ek key
  dnsResolver,
  fetchJson,
});
```

For verification-only use, no local identity configuration or key provider is needed:

```ts
import { createIdentityVerifier } from '@dnsid-ai/sdk';

const verifier = createIdentityVerifier(
  { verification: { trustedEntities: [{ governanceId: 'acme.example' }] } },
  { dnsResolver, fetchJson, logRegistry },
);
const verified = await verifier.verifyDomain('agent.example.com');
```

JOSE, HTTP Message Signature, and OIDC profiles may use this verifier as their
`identityResolver` while omitting `keyProvider`. Their signing or minting methods
then fail with `ArgumentError`.

### Registry-managed C2SP key rotation

`rotateManagedOperationalKey()` composes the registry client, C2SP prepared-event
binding, and operational `KeyProvider`. It validates and signs the registry's
prepared bytes, then activates the pending key only after the registry accepts
those exact bytes. This does not change the generic/self-managed
`IdentityManager.rotateOperationalKey()` workflow.

If the result is pending, or `ManagedKeyRotationSubmissionError` reports
`retryWithSameBytes`, persist its rotation state, pause new application signing,
and call `resumeManagedOperationalKeyRotation()` with that exact state. Do not
generate a replacement event or idempotency key.

If `ManagedKeyRotationActivationError` is raised, registry acceptance succeeded
but local key-state reconciliation is incomplete. Resume with the error's exact
rotation state; already-completed activation or supersession is not repeated.

## Node.js convenience usage

```ts
import { configFromEnvironment, createNodeIdentityManager, LocalKeyProvider } from '@dnsid-ai/sdk/node';

const { config, keyStorePath } = configFromEnvironment();
const keyProvider = await LocalKeyProvider.load(keyStorePath ?? '.dnsid/keys.json', true);
const entityKeyProvider = await LocalKeyProvider.load('.dnsid/entity.keys.json', true);
const idm = await createNodeIdentityManager(config, { keyProvider, entityKeyProvider });
```

`config.transport.dnsServer`/`caBundlePath` configure only the SDK-managed default resolver and
fetcher; a setting is rejected when every dependency it would configure is injected.

`LocalKeyProvider.load(path)` loads an existing store; pass `true` to create one when missing.
Use only one provider instance/process per store; mutations within that instance are serialized.
Persistence uses flushed, mode-0600 sibling files and atomic replacement, keeping the previous
successful generation at `<path>.bak`. Existing symlinks are resolved at load time; mutations and
backups use the resolved target path without replacing the link. The filesystem must support atomic rename, hard links,
and directory fsync. Protect and exclude the store, backup, and sibling `*.tmp` files from source
control; backups and crash-leftover temp files contain private keys. This is not an off-host backup.
If a mutation fails before replacement, the live store and in-memory keys are unchanged. A directory
fsync failure after replacement is reported, but memory follows the now-visible file; inspect it
before retrying. Recovery is manual: stop all users of the store, preserve both files, and validate
the backup against the registry/log state before restoring it. A backup can predate key activation
or contain a superseded key; it is never automatically loaded.
For `dnsid-draft-01` publishing, set `DNSID_EK_URL` to the accountable-entity JWKS URL and `DNSID_KU_URL` to the operational JWKS URL.
The Node helper uses the system or configured DNS resolver by default. It reports `UNKNOWN`, which the default `auto` policy permits and preserves. Inject a DNSSEC-aware resolver for `validated` or `required` policy.

If the registry CLI has already written `~/.dnsid/config.json` and `~/.dnsid/<fqdn>/private.jwk`:

```ts
import { createNodeIdentityManagerFromDnsid } from '@dnsid-ai/sdk/node';

const idm = await createNodeIdentityManagerFromDnsid();
```

DNSSEC modes are: `auto` (default), which rejects `FAILED` and permits `VALID`, `UNSIGNED`, or `UNKNOWN`; `validated`, which permits `VALID` or `UNSIGNED`; and `required`, which permits only `VALID`.

## Included surfaces

`@dnsid-ai/sdk` re-exports the common core and profile surfaces and namespaces:

- `@dnsid-ai/protocol`
- `@dnsid-ai/jose`
- `@dnsid-ai/http-signatures`
- `@dnsid-ai/registry`

Use DNSid OIDC token minting and verification through `@dnsid-ai/oidc`. It is intentionally not re-exported here because its default transport is Node-bound; private-key token minting belongs in server-side code, not browser/client code.

Use lower-level packages directly when you need narrower dependencies or custom composition.

## Modules

- [index](#)
- [node](https://docs.dnsid.ai/reference/ts/dnsid-node/)


Runtime-neutral SDK entry point for DNSid.

`@dnsid-ai/sdk` aggregates the DNSid TypeScript packages: it re-exports
the full protocol core (`@dnsid-ai/protocol`), the registry client and
publishing helpers, the JOSE, HTTP message signatures, and web bot auth
profiles, and managed operational key rotation workflows.

This root entry point makes no runtime assumptions: callers inject DNS resolution,
JSON fetching, key-provider, cache, and log implementations (see
[createIdentityManager](#createidentitymanager)). Node conveniences — LocalKeyProvider, environment
config loading, and Node identity manager factories — live behind the
`@dnsid-ai/sdk/node` subpath. OIDC support deliberately lives in
`@dnsid-ai/oidc` (Node-bound transport) and is not re-exported here.

## Namespaces

- [httpSignatures](https://docs.dnsid.ai/reference/ts/dnsid-http-signatures/)
- [jose](https://docs.dnsid.ai/reference/ts/dnsid-jose/)
- [registry](https://docs.dnsid.ai/reference/ts/dnsid-registry/)
- [webBotAuth](https://docs.dnsid.ai/reference/ts/dnsid-web-bot-auth/)

## Enumerations

<a id="dnssecmode"></a>

### DNSSECMode

Defined in: [packages/protocol/src/types.ts:8](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L8)

#### Enumeration Members

<a id="auto"></a>

##### auto

```ts
auto: "auto";
```

Defined in: [packages/protocol/src/types.ts:9](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L9)

<a id="required"></a>

##### required

```ts
required: "required";
```

Defined in: [packages/protocol/src/types.ts:11](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L11)

<a id="validated"></a>

##### validated

```ts
validated: "validated";
```

Defined in: [packages/protocol/src/types.ts:10](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L10)

***

<a id="dnssecstate"></a>

### DNSSECState

Defined in: [packages/protocol/src/types.ts:1](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L1)

#### Enumeration Members

<a id="failed"></a>

##### FAILED

```ts
FAILED: "FAILED";
```

Defined in: [packages/protocol/src/types.ts:4](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L4)

<a id="unknown"></a>

##### UNKNOWN

```ts
UNKNOWN: "UNKNOWN";
```

Defined in: [packages/protocol/src/types.ts:5](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L5)

<a id="unsigned"></a>

##### UNSIGNED

```ts
UNSIGNED: "UNSIGNED";
```

Defined in: [packages/protocol/src/types.ts:2](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L2)

<a id="valid"></a>

##### VALID

```ts
VALID: "VALID";
```

Defined in: [packages/protocol/src/types.ts:3](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L3)

***

<a id="verificationcode"></a>

### VerificationCode

Defined in: [packages/protocol/src/errors.ts:2](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L2)

Machine-readable classification of a DNSid verification failure, carried on [VerificationError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#verificationerror).

#### Enumeration Members

<a id="counterpartynotaccepted"></a>

##### CounterpartyNotAccepted

```ts
CounterpartyNotAccepted: "CounterpartyNotAccepted";
```

Defined in: [packages/protocol/src/errors.ts:22](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L22)

Configured `trustedEntities` policy denied a protocol-valid counterparty. Always permanent.

<a id="dnsresolution"></a>

##### DNSResolution

```ts
DNSResolution: "DNSResolution";
```

Defined in: [packages/protocol/src/errors.ts:4](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L4)

DNS lookup of the `_dnsid` TXT record failed or returned no identity record. Absence alone is not classified as transient.

<a id="dnssecfailed"></a>

##### DNSSECFailed

```ts
DNSSECFailed: "DNSSECFailed";
```

Defined in: [packages/protocol/src/errors.ts:6](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L6)

DNSSEC validation failed, or the zone is unsigned when the configured DNSSEC mode requires signing.

<a id="keyageexceeded"></a>

##### KeyAgeExceeded

```ts
KeyAgeExceeded: "KeyAgeExceeded";
```

Defined in: [packages/protocol/src/errors.ts:14](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L14)

The operational key is older than the identity record's `ka` maximum key age.

<a id="logerror"></a>

##### LogError

```ts
LogError: "LogError";
```

Defined in: [packages/protocol/src/errors.ts:20](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L20)

A transparency log read, entry check, or consistency verification failed.

<a id="recordinvalid"></a>

##### RecordInvalid

```ts
RecordInvalid: "RecordInvalid";
```

Defined in: [packages/protocol/src/errors.ts:8](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L8)

The identity record or a fetched JWKS is malformed or fails protocol validation.

<a id="signatureinvalid"></a>

##### SignatureInvalid

```ts
SignatureInvalid: "SignatureInvalid";
```

Defined in: [packages/protocol/src/errors.ts:10](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L10)

The identity record signature (or a bilateral binding signature) does not verify against the entity key.

<a id="statusnotactive"></a>

##### StatusNotActive

```ts
StatusNotActive: "StatusNotActive";
```

Defined in: [packages/protocol/src/errors.ts:18](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L18)

The agent status document reports a state other than active (e.g. revoked or retired).

<a id="statusunavailable"></a>

##### StatusUnavailable

```ts
StatusUnavailable: "StatusUnavailable";
```

Defined in: [packages/protocol/src/errors.ts:16](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L16)

The agent status endpoint is unreachable or returned an unusable response.

<a id="tlserror"></a>

##### TLSError

```ts
TLSError: "TLSError";
```

Defined in: [packages/protocol/src/errors.ts:12](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L12)

An HTTPS fetch of the JWKS or status endpoint failed at the transport/TLS layer.

## Type Aliases

<a id="agentstatusstate-1"></a>

### AgentStatusState

```ts
type AgentStatusState = 
  | "PENDING"
  | "PROVISIONING"
  | "VERIFYING"
  | "ACTIVE"
  | "RETIRED"
  | "REVOKED";
```

Defined in: [packages/protocol/src/types.ts:14](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L14)

***

<a id="createidentitymanagerdependencies"></a>

### CreateIdentityManagerDependencies

```ts
type CreateIdentityManagerDependencies = IdentityManagerDependencies & Required<Pick<IdentityManagerDependencies, "dnsResolver" | "fetchJson">>;
```

Defined in: [packages/sdk/src/index.ts:102](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/index.ts#L102)

Runtime-neutral dependencies: DNS and JSON fetching must be injected.

***

<a id="jsonfetcher"></a>

### JsonFetcher

```ts
type JsonFetcher = (url, opts?) => Promise<FetchResult>;
```

Defined in: [packages/protocol/src/identity-manager.ts:81](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L81)

#### Parameters

##### url

`string`

##### opts?

[`JsonFetchOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#jsonfetchoptions)

#### Returns

`Promise`\<[`FetchResult`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#fetchresult)\>

***

<a id="lifecycleerrorcategory"></a>

### LifecycleErrorCategory

```ts
type LifecycleErrorCategory = 
  | "GENESIS_REQUIRED"
  | "DUPLICATE_ISSUANCE"
  | "INVALID_ISSUANCE"
  | "TERMINAL_STATE"
  | "DOMAIN_MISMATCH"
  | "KEY_CONTINUITY"
  | "INVALID_REVOCATION_REASON"
  | "INVALID_MIGRATION"
  | "SNAPSHOT_EMPTY"
  | "SNAPSHOT_NON_PREFIX"
  | "UNSUPPORTED_EVENT"
  | "CHAIN_CONTINUITY"
  | "INVALID_EVIDENCE"
  | "INCOMPLETE_STREAM";
```

Defined in: [packages/protocol/src/errors.ts:26](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/errors.ts#L26)

Stable, language-neutral categories used by lifecycle conformance vectors.

***

<a id="logevent"></a>

### LogEvent

```ts
type LogEvent = 
  | IssuanceEvent
  | KeyRotationEvent
  | RevocationEvent
  | RetirementEvent
  | MigrationEvent
  | DelegationEvent;
```

Defined in: [packages/protocol/src/log-events.ts:122](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/log-events.ts#L122)

***

<a id="logeventtype"></a>

### LogEventType

```ts
type LogEventType = LogEvent["type"];
```

Defined in: [packages/protocol/src/log-events.ts:130](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/log-events.ts#L130)

***

<a id="logref-2"></a>

### LogRef

```ts
type LogRef = string;
```

Defined in: [packages/protocol/src/types.ts:55](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L55)

***

<a id="logsignerrole"></a>

### LogSignerRole

```ts
type LogSignerRole = 
  | "Entity"
  | "Operational"
  | "OperationalCountersignature"
  | "PreviousOperational"
  | "NewOperational";
```

Defined in: [packages/protocol/src/log.ts:4](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/log.ts#L4)

***

<a id="managedissuancesubmission"></a>

### ManagedIssuanceSubmission

```ts
type ManagedIssuanceSubmission = 
  | SubmissionResult
  | {
  entryHash: string;
  errorCode?: string;
  state: "indeterminate";
};
```

Defined in: [packages/sdk/src/managed-issuance.ts:44](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/managed-issuance.ts#L44)

***

<a id="maxkeyage-1"></a>

### MaxKeyAge

```ts
type MaxKeyAge = "24h" | "7d" | "30d" | "90d";
```

Defined in: [packages/protocol/src/types.ts:28](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L28)

***

<a id="revocationreason-1"></a>

### RevocationReason

```ts
type RevocationReason = 
  | "keyCompromise"
  | "policyViolation"
  | "superseded"
  | "cessationOfOperation";
```

Defined in: [packages/protocol/src/types.ts:22](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/types.ts#L22)

## Variables

<a id="default_publish_profile"></a>

### DEFAULT\_PUBLISH\_PROFILE

```ts
const DEFAULT_PUBLISH_PROFILE: "dnsid-draft-01" = DNSID_DRAFT01_VERSION;
```

Defined in: [packages/protocol/src/txt-record.ts:8](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/txt-record.ts#L8)

***

<a id="dnsid_draft01_version"></a>

### DNSID\_DRAFT01\_VERSION

```ts
const DNSID_DRAFT01_VERSION: "dnsid-draft-01" = 'dnsid-draft-01';
```

Defined in: [packages/protocol/src/txt-record.ts:7](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/txt-record.ts#L7)

Immutable selector for submitted draft-ihsanullah-dnsid-01.

***

<a id="dnsid_version"></a>

### DNSID\_VERSION

```ts
const DNSID_VERSION: "DNSid1" = 'DNSid1';
```

Defined in: [packages/protocol/src/txt-record.ts:5](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/txt-record.ts#L5)

Pre-RFC moving verification selector. Never published while version 1 is a draft.

***

<a id="jwks_max_response_bytes"></a>

### JWKS\_MAX\_RESPONSE\_BYTES

```ts
const JWKS_MAX_RESPONSE_BYTES: number;
```

Defined in: [packages/protocol/src/identity-manager.ts:71](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L71)

***

<a id="sdk_conformance"></a>

### SDK\_CONFORMANCE

```ts
const SDK_CONFORMANCE: SDKConformance;
```

Defined in: [packages/sdk/src/conformance.ts:24](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/conformance.ts#L24)

Exact DNSid profile and log-binding behavior implemented by this SDK release.

***

<a id="sha256bytes"></a>

### sha256Bytes

```ts
const sha256Bytes: TRet<CHash<_SHA256>>;
```

Defined in: node\_modules/@noble/hashes/sha2.d.ts:113

SHA2-256 hash function from RFC 4634. In JS it's the fastest: even faster than Blake3. Some info:

- Trying 2^128 hashes would get 50% chance of collision, using birthday attack.
- BTC network is doing 2^70 hashes/sec (2^95 hashes/year) as per 2025.
- Each sha256 hash is executing 2^18 bit operations.
- Good 2024 ASICs can do 200Th/sec with 3500 watts of power, corresponding to 2^36 hashes/joule.

#### Param

**msg**

message bytes to hash

#### Param

**opts**

Reserved hash options.

#### Returns

Digest bytes.

#### Example

Hash a message with SHA2-256.
```ts
sha256(new Uint8Array([97, 98, 99]));
```

***

<a id="signing_algs"></a>

### SIGNING\_ALGS

```ts
const SIGNING_ALGS: Set<string>;
```

Defined in: [packages/protocol/src/jwks.ts:6](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/jwks.ts#L6)

***

<a id="status_max_response_bytes"></a>

### STATUS\_MAX\_RESPONSE\_BYTES

```ts
const STATUS_MAX_RESPONSE_BYTES: number;
```

Defined in: [packages/protocol/src/identity-manager.ts:72](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L72)

***

<a id="supported_publish_profiles"></a>

### SUPPORTED\_PUBLISH\_PROFILES

```ts
const SUPPORTED_PUBLISH_PROFILES: readonly ["dnsid-draft-01"];
```

Defined in: [packages/protocol/src/txt-record.ts:9](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/txt-record.ts#L9)

***

<a id="supported_validation_profiles"></a>

### SUPPORTED\_VALIDATION\_PROFILES

```ts
const SUPPORTED_VALIDATION_PROFILES: readonly ["dnsid-draft-01", "DNSid1"];
```

Defined in: [packages/protocol/src/txt-record.ts:10](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/txt-record.ts#L10)

## Functions

<a id="activestatusdocument"></a>

### activeStatusDocument()

```ts
function activeStatusDocument(lastTransitionAt?): AgentStatus;
```

Defined in: [packages/protocol/src/agent-status.ts:16](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/agent-status.ts#L16)

Builds a simple ACTIVE status document for demos/tests that do not model
lifecycle state.

#### Parameters

##### lastTransitionAt?

`Date` = `...`

#### Returns

[`AgentStatus`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#agentstatus)

***

<a id="canonicalissuancebinding"></a>

### canonicalIssuanceBinding()

```ts
function canonicalIssuanceBinding(event): Uint8Array;
```

Defined in: [packages/protocol/src/identity-manager.ts:1059](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L1059)

Canonical bytes covered by BOTH the entity signature and the operational
countersignature of a draft-01 bilateral ISSUANCE event. Both signatures
MUST cover identical content, so this is the single source of that content.

ponytail: minimal, fixed-order line encoding — no JSON key-ordering traps.
Replace with the spec's canonicalization/test vectors once they land.

#### Parameters

##### event

[`IssuanceEvent`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#issuanceevent)

#### Returns

`Uint8Array`

***

<a id="createidentitymanager"></a>

### createIdentityManager()

```ts
function createIdentityManager(config, deps): IdentityManager;
```

Defined in: [packages/sdk/src/index.ts:126](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/index.ts#L126)

Creates a DNSid IdentityManager from explicitly injected runtime dependencies.

The root `@dnsid-ai/sdk` entrypoint is runtime-neutral: callers provide DNS, HTTPS/JSON
fetching, key storage/signing, cache, and log implementations appropriate for Node,
browsers, workers, wallets, HSMs, or application backends. Omit `config.identity` for a
verification-only manager (see [createIdentityVerifier](#createidentityverifier)).

#### Parameters

##### config

[`DnsidConfig`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidconfig)

##### deps

[`CreateIdentityManagerDependencies`](#createidentitymanagerdependencies)

#### Returns

[`IdentityManager`](https://docs.dnsid.ai/reference/ts/dnsid-classes/#identitymanager)

#### Example

```ts
import { createIdentityManager } from '@dnsid-ai/sdk';

const idm = createIdentityManager({ identity, verification }, {
  keyProvider,       // operational key
  entityKeyProvider, // entity key
  dnsResolver,       // DNSSEC-aware resolver
  fetchJson,
});
const verified = await idm.verifyDomain('agent.example.com');
```

***

<a id="createidentityverifier"></a>

### createIdentityVerifier()

```ts
function createIdentityVerifier(config, deps): IdentityManager;
```

Defined in: [packages/sdk/src/index.ts:136](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/index.ts#L136)

Creates a verification-only IdentityManager: same constructor, `config.identity` omitted.

The returned manager supports identity verification, verified log loading, and cache
eviction. Local signing, publication, and lifecycle mutation methods throw ArgumentError.

#### Parameters

##### config

`Omit`\<[`DnsidConfig`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidconfig), `"identity"`\>

##### deps

`Omit`\<[`CreateIdentityManagerDependencies`](#createidentitymanagerdependencies), `"keyProvider"` \| `"entityKeyProvider"`\>

#### Returns

[`IdentityManager`](https://docs.dnsid.ai/reference/ts/dnsid-classes/#identitymanager)

***

<a id="frombase64url"></a>

### fromBase64Url()

```ts
function fromBase64Url(b64): Uint8Array;
```

Defined in: [packages/protocol/src/utils.ts:140](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L140)

Decodes a base64url string (accepts both padded and unpadded forms) to Uint8Array.

#### Parameters

##### b64

`string`

#### Returns

`Uint8Array`

***

<a id="isdomainname"></a>

### isDomainName()

```ts
function isDomainName(value): boolean;
```

Defined in: [packages/protocol/src/utils.ts:80](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L80)

Checks whether a string is a valid domain name (for gi consistency checks).
Returns true if the value looks like a domain name (as opposed to a URI or other identifier).

#### Parameters

##### value

`string`

#### Returns

`boolean`

***

<a id="issuemanagedidentity"></a>

### issueManagedIdentity()

```ts
function issueManagedIdentity(options): Promise<ManagedIssuanceState>;
```

Defined in: [packages/sdk/src/managed-issuance.ts:109](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/managed-issuance.ts#L109)

Starts one durable managed setup operation, or resumes the already persisted one.

#### Parameters

##### options

[`IssueManagedIdentityOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#issuemanagedidentityoptions)

#### Returns

`Promise`\<[`ManagedIssuanceState`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedissuancestate)\>

***

<a id="istransientverificationerror"></a>

### isTransientVerificationError()

```ts
function isTransientVerificationError(error): boolean;
```

Defined in: [packages/protocol/src/retry.ts:26](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/retry.ts#L26)

Returns true when the error is a transient DNSid verification failure.

#### Parameters

##### error

`unknown`

#### Returns

`boolean`

***

<a id="jwksignaturealg"></a>

### jwkSignatureAlg()

```ts
function jwkSignatureAlg(key): string;
```

Defined in: [packages/protocol/src/jwks.ts:37](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/jwks.ts#L37)

#### Parameters

##### key

[`DnsIdJWK`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidjwk)

#### Returns

`string`

***

<a id="jwkthumbprint"></a>

### jwkThumbprint()

```ts
function jwkThumbprint(key): Promise<string>;
```

Defined in: [packages/protocol/src/jwks.ts:218](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/jwks.ts#L218)

Computes the RFC 7638 JWK thumbprint of a key.
Returns unpadded base64url (RFC 7515 §2).

Lifecycle log bindings MUST use thumbprints, not kid values, as the durable key identifier.

#### Parameters

##### key

[`DnsIdJWK`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidjwk)

#### Returns

`Promise`\<`string`\>

***

<a id="keysetssharekeymaterial"></a>

### keySetsShareKeyMaterial()

```ts
function keySetsShareKeyMaterial(a, b): Promise<boolean>;
```

Defined in: [packages/protocol/src/jwks.ts:247](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/jwks.ts#L247)

Returns true if any key in `a` shares an RFC 7638 JWK thumbprint with any key in `b`.

draft-01 §Two-Key Separation requires the ek and ku JWK Sets to be pairwise thumbprint-disjoint,
even for self-accounted DNSids — a verifier MUST NOT infer self-accounting from key equality,
so this check does not special-case any relationship between the two sets.

Throws a normalized ValidationError if any key in either set is too malformed to thumbprint.

#### Parameters

##### a

[`JWKS`](https://docs.dnsid.ai/reference/ts/dnsid-classes/#jwks)

##### b

[`JWKS`](https://docs.dnsid.ai/reference/ts/dnsid-classes/#jwks)

#### Returns

`Promise`\<`boolean`\>

***

<a id="matchesdnsname"></a>

### matchesDnsName()

```ts
function matchesDnsName(san, fqdn): boolean;
```

Defined in: [packages/protocol/src/utils.ts:198](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L198)

RFC 9525 §4 dNSName SAN matching.
Returns true when at least one SAN entry matches the given FQDN.
Supports case-insensitive comparison, trailing-dot normalisation, and
wildcard labels (only leftmost `*.` matching one or more labels at depth > 0).

#### Parameters

##### san

`string`[]

##### fqdn

`string`

#### Returns

`boolean`

***

<a id="normalizefqdn"></a>

### normalizeFQDN()

```ts
function normalizeFQDN(name, agentFQDN?): string;
```

Defined in: [packages/protocol/src/utils.ts:18](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L18)

Converts IDNA U-labels to A-label punycode, lowercases ASCII, strips one trailing root dot,
and validates DNS label constraints.

#### Parameters

##### name

`string`

##### agentFQDN?

`boolean` = `false`

#### Returns

`string`

#### Throws

ValidationError if the name is empty, contains empty labels, has any label over 63 octets,
        exceeds the 253-octet DNS limit, or (when agentFQDN=true) exceeds the 246-octet DNSid agent limit.

***

<a id="parsecompactjose"></a>

### parseCompactJose()

```ts
function parseCompactJose(token): object;
```

Defined in: [packages/protocol/src/strict-json.ts:31](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/strict-json.ts#L31)

Standalone JOSE limits: 1 MiB compact token, 16 KiB encoded header.

#### Parameters

##### token

`string`

#### Returns

`object`

##### header

```ts
header: Record<string, unknown>;
```

##### parts

```ts
parts: [string, string, string];
```

##### payload

```ts
payload: Uint8Array;
```

##### signature

```ts
signature: Uint8Array;
```

***

<a id="parsejoseobject"></a>

### parseJoseObject()

```ts
function parseJoseObject(bytes): Record<string, unknown>;
```

Defined in: [packages/protocol/src/strict-json.ts:52](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/strict-json.ts#L52)

#### Parameters

##### bytes

`Uint8Array`

#### Returns

`Record`\<`string`, `unknown`\>

***

<a id="parsejsonnoduplicatemembers"></a>

### parseJsonNoDuplicateMembers()

```ts
function parseJsonNoDuplicateMembers(bytes): unknown;
```

Defined in: [packages/protocol/src/strict-json.ts:5](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/strict-json.ts#L5)

UTF-8 JSON with duplicate member rejection, including escaped member names.

#### Parameters

##### bytes

`Uint8Array`

#### Returns

`unknown`

***

<a id="parsekaduration"></a>

### parseKaDuration()

```ts
function parseKaDuration(ka): number;
```

Defined in: [packages/protocol/src/utils.ts:156](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L156)

Parses a duration string (as used in the `ka` tag) to milliseconds.
Valid values: "24h", "7d", "30d", "90d".

#### Parameters

##### ka

`string`

#### Returns

`number`

***

<a id="parsekeyid"></a>

### parseKeyId()

```ts
function parseKeyId(keyId): object;
```

Defined in: [packages/protocol/src/utils.ts:100](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L100)

Parses the DNSid SDK's cross-profile compound key ID convention: "{domain}#{kid}".

This is an SDK/profile convention used by packages such as @dnsid-ai/jose and
@dnsid-ai/http-signatures to bind a profile-level key reference to a DNSid agent
FQDN plus a JWKS "kid". It is not a DNSid protocol wire-format requirement;
the protocol itself only requires JWKS keys to carry "kid" values.

Splits on the first '#', normalizes the domain side with normalizeFQDN(),
and rejects if either side is empty or the kid side contains another '#'.

#### Parameters

##### keyId

`string`

#### Returns

`object`

##### domain

```ts
domain: string;
```

##### kid

```ts
kid: string;
```

#### Throws

ArgumentError if the key ID is malformed.

***

<a id="requirelocaldomain"></a>

### requireLocalDomain()

```ts
function requireLocalDomain(manager): string;
```

Defined in: [packages/protocol/src/identity-manager.ts:105](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L105)

Returns `config.identity.domain` or throws `ArgumentError` for verification-only managers.

#### Parameters

##### manager

[`SigningIdentityManager`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#signingidentitymanager)

#### Returns

`string`

***

<a id="resumemanagedissuance"></a>

### resumeManagedIssuance()

```ts
function resumeManagedIssuance(options): Promise<ManagedIssuanceState>;
```

Defined in: [packages/sdk/src/managed-issuance.ts:143](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/managed-issuance.ts#L143)

Resumes only the durable operation, reusing its preparation key or exact completed bytes.

#### Parameters

##### options

[`ResumeManagedIssuanceOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#resumemanagedissuanceoptions)

#### Returns

`Promise`\<[`ManagedIssuanceState`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedissuancestate)\>

***

<a id="resumemanagedoperationalkeyrotation"></a>

### resumeManagedOperationalKeyRotation()

```ts
function resumeManagedOperationalKeyRotation(options): Promise<ManagedKeyRotationResult>;
```

Defined in: [packages/sdk/src/managed-key-rotation.ts:161](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/managed-key-rotation.ts#L161)

Retries only the persisted completed bytes and activates on acceptance.

#### Parameters

##### options

[`ResumeManagedOperationalKeyRotationOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#resumemanagedoperationalkeyrotationoptions)

#### Returns

`Promise`\<[`ManagedKeyRotationResult`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedkeyrotationresult)\>

***

<a id="retrytransientverification"></a>

### retryTransientVerification()

```ts
function retryTransientVerification<T>(operation, options?): Promise<T>;
```

Defined in: [packages/protocol/src/retry.ts:35](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/retry.ts#L35)

Retries an operation using exponential backoff, but only for transient
VerificationError failures by default. Integrity and policy failures are never
retried unless callers explicitly override shouldRetry.

#### Type Parameters

##### T

`T`

#### Parameters

##### operation

() => `Promise`\<`T`\>

##### options?

[`RetryBackoffOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#retrybackoffoptions) = `{}`

#### Returns

`Promise`\<`T`\>

***

<a id="rotatemanagedoperationalkey"></a>

### rotateManagedOperationalKey()

```ts
function rotateManagedOperationalKey(options): Promise<ManagedKeyRotationResult>;
```

Defined in: [packages/sdk/src/managed-key-rotation.ts:99](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/sdk/src/managed-key-rotation.ts#L99)

Runs C2SP registry-managed rotation. The registry owns managed ku publication
and append reconciliation; local activation happens only after an accepted
result for the exact, durably persisted bytes while application signing is paused.

#### Parameters

##### options

[`RotateManagedOperationalKeyOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#rotatemanagedoperationalkeyoptions)

#### Returns

`Promise`\<[`ManagedKeyRotationResult`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedkeyrotationresult)\>

***

<a id="toarraybuffer"></a>

### toArrayBuffer()

```ts
function toArrayBuffer(bytes): Uint8Array<ArrayBuffer>;
```

Defined in: [packages/protocol/src/utils.ts:171](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L171)

Returns a Uint8Array<ArrayBuffer> view over the same memory — no copy.
Required because WebCrypto's BufferSource only accepts ArrayBuffer-backed views,
not the default Uint8Array<ArrayBufferLike> that TypeScript infers.

#### Parameters

##### bytes

`Uint8Array`

#### Returns

`Uint8Array`\<`ArrayBuffer`\>

***

<a id="tobase64url"></a>

### toBase64Url()

```ts
function toBase64Url(bytes): string;
```

Defined in: [packages/protocol/src/utils.ts:128](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L128)

Encodes a Uint8Array to unpadded base64url (RFC 7515 §2).

#### Parameters

##### bytes

`Uint8Array`

#### Returns

`string`

***

<a id="validateagentstatus"></a>

### validateAgentStatus()

```ts
function validateAgentStatus(data): AgentStatus;
```

Defined in: [packages/protocol/src/agent-status.ts:23](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/agent-status.ts#L23)

Validates the DNSid JSON status profile returned by the `su` endpoint.

#### Parameters

##### data

`unknown`

#### Returns

[`AgentStatus`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#agentstatus)

***

<a id="validatednsidconfig"></a>

### validateDnsidConfig()

```ts
function validateDnsidConfig(config?): DnsidConfig;
```

Defined in: [packages/protocol/src/identity-manager.ts:273](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L273)

Validates and snapshots a [DnsidConfig](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidconfig). Shared by every constructor and loader so all
initialization paths apply identical defaults and rejections.

#### Parameters

##### config?

`unknown` = `{}`

#### Returns

[`DnsidConfig`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidconfig)

***

<a id="verifybilateralbinding-2"></a>

### verifyBilateralBinding()

```ts
function verifyBilateralBinding(
   event, 
   record, 
   currentEntityKey
): Promise<{
  initialOperationalThumbprint: string;
}>;
```

Defined in: [packages/protocol/src/identity-manager.ts:1096](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/identity-manager.ts#L1096)

draft-01 step-5 bilateral binding check. ISSUANCE is bilateral: it is only
valid when BOTH the accountable-entity record-signing key (ek) and the initial
operational key (ku) signed the same canonical binding, and that binding
corresponds to the TXT record and current key material.

Verifies, against key material recorded IN THE EVENT:
 1. Each slot's `thumbprint` equals `jwkThumbprint(jwk)` (the canonical binding
    only commits to the thumbprint, so the embedded JWK must be pinned to it).
 2. `entitySig` under `entityKey`, and `operationalSig` under `operationalKey`.
 3. Same DNSid FQDN and same `gi` as the record.
 4. The ISSUANCE-recorded entity key is the key currently at `ek`.

It does NOT reject a rotated `ku`: the current operational key may be the
initial key OR a key linked to it by KEY_ROTATION continuity. That linkage
(and entity KEY_ROTATION linkage) is verified separately by
LogReader.verifyOperationalContinuity, which runs unconditionally in the
draft-01 path of verifyDomain. The recorded initial operational thumbprint is
returned so the caller can hand it to that continuity check.

#### Parameters

##### event

[`IssuanceEvent`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#issuanceevent)

##### record

[`DnsIdTxtRecord`](https://docs.dnsid.ai/reference/ts/dnsid-classes/#dnsidtxtrecord)

##### currentEntityKey

[`DnsIdJWK`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidjwk)

#### Returns

`Promise`\<\{
  `initialOperationalThumbprint`: `string`;
\}\>

***

<a id="verifywithkey"></a>

### verifyWithKey()

```ts
function verifyWithKey(
   signingInput, 
   signature, 
   key, 
   expectedAlg?
): Promise<boolean>;
```

Defined in: [packages/protocol/src/utils.ts:218](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/utils.ts#L218)

Verifies a signing input against a raw signature using a public JWK.
Returns true if the signature is valid, false otherwise.

#### Parameters

##### signingInput

`string` \| `Uint8Array`\<`ArrayBufferLike`\>

##### signature

`Uint8Array`

##### key

[`DnsIdJWK`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidjwk)

##### expectedAlg?

`string`

#### Returns

`Promise`\<`boolean`\>

***

<a id="waitforverification"></a>

### waitForVerification()

```ts
function waitForVerification<T>(operation, signal): Promise<T>;
```

Defined in: [packages/protocol/src/verification-budget.ts:43](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/verification-budget.ts#L43)

Races cooperative work against cancellation, including already-aborted invocations.

#### Type Parameters

##### T

`T`

#### Parameters

##### operation

(`signal`) => `Promise`\<`T`\>

##### signal

`AbortSignal`

#### Returns

`Promise`\<`T`\>

***

<a id="withverificationbudget"></a>

### withVerificationBudget()

```ts
function withVerificationBudget<T>(operation, options?): Promise<T>;
```

Defined in: [packages/protocol/src/verification-budget.ts:10](https://github.com/dnsid-ai/dnsid-ts/blob/main/packages/protocol/src/verification-budget.ts#L10)

Runs an invocation with a shared cancellation signal; child operations must not restart its budget.

#### Type Parameters

##### T

`T`

#### Parameters

##### operation

(`signal`) => `Promise`\<`T`\>

##### options?

[`VerificationOptions`](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#verificationoptions) = `{}`

#### Returns

`Promise`\<`T`\>

## References

<a id="awaitregistrymanagedpublication"></a>

### awaitRegistryManagedPublication

Re-exports [awaitRegistryManagedPublication](https://docs.dnsid.ai/reference/ts/dnsid-registry/#awaitregistrymanagedpublication)

***

<a id="createhttpsignaturesprofile"></a>

### createHttpSignaturesProfile

Re-exports [createHttpSignaturesProfile](https://docs.dnsid.ai/reference/ts/dnsid-http-signatures/#createhttpsignaturesprofile)

***

<a id="createjoseprofile"></a>

### createJoseProfile

Re-exports [createJoseProfile](https://docs.dnsid.ai/reference/ts/dnsid-jose/#createjoseprofile)

***

<a id="createwebbotauthprofile"></a>

### createWebBotAuthProfile

Re-exports [createWebBotAuthProfile](https://docs.dnsid.ai/reference/ts/dnsid-web-bot-auth/#createwebbotauthprofile)

***

<a id="default_registry_url"></a>

### DEFAULT\_REGISTRY\_URL

Re-exports [DEFAULT_REGISTRY_URL](https://docs.dnsid.ai/reference/ts/dnsid-registry/#default_registry_url)

***

<a id="httpsignaturesprofile"></a>

### HttpSignaturesProfile

Re-exports [HttpSignaturesProfile](https://docs.dnsid.ai/reference/ts/dnsid-http-signatures/#httpsignaturesprofile)

***

<a id="joseprofile"></a>

### JoseProfile

Re-exports [JoseProfile](https://docs.dnsid.ai/reference/ts/dnsid-jose/#joseprofile)

***

<a id="preparedeventsubmissionerror"></a>

### PreparedEventSubmissionError

Re-exports [PreparedEventSubmissionError](https://docs.dnsid.ai/reference/ts/dnsid-registry/#preparedeventsubmissionerror)

***

<a id="publishclientcontrolledrecord"></a>

### publishClientControlledRecord

Re-exports [publishClientControlledRecord](https://docs.dnsid.ai/reference/ts/dnsid-registry/#publishclientcontrolledrecord)

***

<a id="publishtoregistry"></a>

### publishToRegistry

Re-exports [publishToRegistry](https://docs.dnsid.ai/reference/ts/dnsid-registry/#publishtoregistry)

***

<a id="registryclient-4"></a>

### RegistryClient

Re-exports [RegistryClient](https://docs.dnsid.ai/reference/ts/dnsid-registry/#registryclient)

***

<a id="registryworkflowerror"></a>

### RegistryWorkflowError

Re-exports [RegistryWorkflowError](https://docs.dnsid.ai/reference/ts/dnsid-registry/#registryworkflowerror)

***

<a id="webbotauthprofile"></a>

### WebBotAuthProfile

Re-exports [WebBotAuthProfile](https://docs.dnsid.ai/reference/ts/dnsid-web-bot-auth/#webbotauthprofile)

## Classes

Documented on [Core SDK: classes](https://docs.dnsid.ai/reference/ts/dnsid-classes/):

- [ArgumentError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#argumenterror)
- [DnsIdTxtRecord](https://docs.dnsid.ai/reference/ts/dnsid-classes/#dnsidtxtrecord)
- [DomainLog](https://docs.dnsid.ai/reference/ts/dnsid-classes/#domainlog)
- [DomainSnapshot](https://docs.dnsid.ai/reference/ts/dnsid-classes/#domainsnapshot)
- [IdentityManager](https://docs.dnsid.ai/reference/ts/dnsid-classes/#identitymanager)
- [InMemoryIdentityCache](https://docs.dnsid.ai/reference/ts/dnsid-classes/#inmemoryidentitycache)
- [JWKS](https://docs.dnsid.ai/reference/ts/dnsid-classes/#jwks)
- [LogRegistry](https://docs.dnsid.ai/reference/ts/dnsid-classes/#logregistry)
- [ManagedIssuanceActivationError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#managedissuanceactivationerror)
- [ManagedIssuanceSubmissionError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#managedissuancesubmissionerror)
- [ManagedKeyRotationActivationError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#managedkeyrotationactivationerror)
- [ManagedKeyRotationSubmissionError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#managedkeyrotationsubmissionerror)
- [NoopLogReader](https://docs.dnsid.ai/reference/ts/dnsid-classes/#nooplogreader)
- [ParseError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#parseerror)
- [ValidationError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#validationerror)
- [VerificationError](https://docs.dnsid.ai/reference/ts/dnsid-classes/#verificationerror)
- [VerifiedDomain](https://docs.dnsid.ai/reference/ts/dnsid-classes/#verifieddomain)

## Interfaces

Documented on [Core SDK: interfaces](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/):

- [AgentStatus](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#agentstatus)
- [C2spIssuanceEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#c2spissuanceevent)
- [DelegationEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#delegationevent)
- [DnsidConfig](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidconfig)
- [DnsIdJWK](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsidjwk)
- [DNSResolver](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#dnsresolver)
- [FetchResult](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#fetchresult)
- [IdentityCache](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#identitycache)
- [IdentityConfig](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#identityconfig)
- [IdentityManagerDependencies](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#identitymanagerdependencies)
- [IdentityResolver](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#identityresolver)
- [IssuanceEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#issuanceevent)
- [IssuanceKeySlot](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#issuancekeyslot)
- [IssueManagedIdentityOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#issuemanagedidentityoptions)
- [JsonFetchOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#jsonfetchoptions)
- [KeyProvider](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#keyprovider-1)
- [KeyRotationEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#keyrotationevent)
- [Log](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#log)
- [LoggedStateEvidence](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#loggedstateevidence)
- [LogReader](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#logreader-1)
- [ManagedIssuanceCoordination](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedissuancecoordination)
- [ManagedIssuanceRegistry](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedissuanceregistry)
- [ManagedIssuanceState](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedissuancestate)
- [ManagedKeyRotationRegistry](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedkeyrotationregistry)
- [ManagedKeyRotationResult](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#managedkeyrotationresult)
- [MigrationEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#migrationevent)
- [OperationalKeyRotationOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#operationalkeyrotationoptions)
- [OperationalKeyRotationResult](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#operationalkeyrotationresult)
- [ResumeManagedIssuanceOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#resumemanagedissuanceoptions)
- [ResumeManagedOperationalKeyRotationOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#resumemanagedoperationalkeyrotationoptions)
- [RetirementEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#retirementevent)
- [RetryBackoffOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#retrybackoffoptions)
- [RevocationEvent](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#revocationevent)
- [RotateManagedOperationalKeyOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#rotatemanagedoperationalkeyoptions)
- [SDKConformance](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#sdkconformance)
- [SigningIdentityManager](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#signingidentitymanager)
- [TLSCertificate](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#tlscertificate)
- [TransportConfig](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#transportconfig)
- [TrustedEntity](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#trustedentity)
- [TXTRecord](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#txtrecord)
- [VerificationConfig](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#verificationconfig)
- [VerificationOptions](https://docs.dnsid.ai/reference/ts/dnsid-interfaces/#verificationoptions)
